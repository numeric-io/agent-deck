---
phase: 01-skills-reorganization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.agent-deck/skills/pool/gsd-conductor/SKILL.md
  - ~/.agent-deck/skills/pool/gsd-conductor/references/gsd-internals.md
autonomous: true
requirements:
  - SKILL-03
  - SKILL-05

must_haves:
  truths:
    - "GSD conductor SKILL.md documents all core GSD lifecycle commands that exist in the project"
    - "GSD conductor SKILL.md frontmatter has name and description (spec-compliant)"
    - "GSD conductor SKILL.md references/gsd-internals.md agent table matches actual GSD agent list"
    - "Script path references in all three skills (agent-deck, session-share, gsd-conductor) use $SKILL_DIR or relative-to-skill patterns, not hardcoded absolute paths"
    - "All three skills pass quick_validate.py"
  artifacts:
    - path: "~/.agent-deck/skills/pool/gsd-conductor/SKILL.md"
      provides: "GSD conductor orchestration skill"
      contains: "name: gsd-conductor"
    - path: "~/.agent-deck/skills/pool/gsd-conductor/references/gsd-internals.md"
      provides: "GSD internal reference documentation"
      contains: "Agent Types"
  key_links:
    - from: "~/.agent-deck/skills/pool/gsd-conductor/SKILL.md"
      to: "references/gsd-internals.md"
      via: "Relative reference from SKILL.md"
      pattern: "references/gsd-internals.md"
---

<objective>
Verify and update the GSD conductor pool skill content against the latest GSD v1.22.4 installation, ensure its SKILL.md is spec-compliant, and validate script path resolution across all three skills in the project.

Purpose: The gsd-conductor skill was last updated Feb 27, 2026. GSD has evolved since then (now v1.22.4 with 32 commands). The skill content must reflect the current state, and path resolution must work from the pool skill location.

Output: Updated gsd-conductor SKILL.md and references/gsd-internals.md with current GSD content, plus validation that all three skills have correct path resolution.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-skills-reorganization/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and update GSD conductor skill content</name>
  <files>~/.agent-deck/skills/pool/gsd-conductor/SKILL.md, ~/.agent-deck/skills/pool/gsd-conductor/references/gsd-internals.md</files>
  <action>
**Step 1: Audit current content against GSD v1.22.4**

Read the following to understand what GSD currently provides:
- `/Users/ashesh/claude-deck/.claude/get-shit-done/VERSION` (version string)
- List commands: `ls /Users/ashesh/claude-deck/.claude/commands/gsd/`
- Read agent workflow files to find agent types: `grep -r "gsd-" /Users/ashesh/claude-deck/.claude/get-shit-done/workflows/ | grep -oP 'gsd-[a-z-]+' | sort -u`

**Step 2: Update SKILL.md**

The gsd-conductor SKILL.md already has correct frontmatter (`name: gsd-conductor`, `description: ...`). No `compatibility` field exists (correct, since this is a pool skill, not a plugin skill). Do NOT add one.

Update the SKILL.md content:
1. Add any missing GSD commands to the lifecycle description. The current SKILL.md covers the 4 core stages (new-project, discuss+plan-phase, execute-phase, verify-work). Check if any important new commands are missing. Key commands to verify are covered: `research-phase`, `progress`, `debug`, `quick`, `new-milestone`, `complete-milestone`. Only add commands that are relevant to conductor orchestration (skip niche/internal ones like `reapply-patches`, `set-profile`, `settings`).
2. Update the `references/gsd-internals.md` reference path if it uses absolute paths (it currently uses relative `references/gsd-internals.md` which is correct).
3. Update the GSD File Structure section if the `.planning/` directory structure has changed.

**Step 3: Update references/gsd-internals.md**

Read the current file and cross-reference against actual GSD agents. Update the Agent Types table if any agents have been added, removed, or renamed since Feb 27. Read relevant GSD workflow files in `.claude/get-shit-done/workflows/` to find current agent definitions.

Update Model Profiles if the config options have changed (check `.claude/get-shit-done/bin/gsd-tools.cjs` or config defaults).

**Important constraints:**
- Do NOT add hardcoded paths (no `/Users/ashesh/...`) in the SKILL.md
- Keep the existing structure and tone
- The skill is at `~/.agent-deck/skills/pool/gsd-conductor/`, NOT in the repo. Use absolute path `/Users/ashesh/.agent-deck/skills/pool/gsd-conductor/` when reading/writing.
  </action>
  <verify>
    <automated>python3 /Users/ashesh/.agent-deck/skills/pool/skill-creator/scripts/quick_validate.py /Users/ashesh/.agent-deck/skills/pool/gsd-conductor && echo "PASS: gsd-conductor validates" && grep -c "gsd:" /Users/ashesh/.agent-deck/skills/pool/gsd-conductor/SKILL.md</automated>
  </verify>
  <done>
- gsd-conductor SKILL.md passes quick_validate.py
- SKILL.md references all core GSD lifecycle commands present in GSD v1.22.4
- references/gsd-internals.md Agent Types table matches actual GSD agents
- No hardcoded absolute paths in SKILL.md content
  </done>
</task>

<task type="auto">
  <name>Task 2: Validate path resolution across all three skills</name>
  <files>skills/agent-deck/SKILL.md, skills/session-share/SKILL.md, ~/.agent-deck/skills/pool/gsd-conductor/SKILL.md</files>
  <action>
Run validation on all three skills and verify path resolution patterns:

1. **Run quick_validate.py on all three skills:**
```bash
python3 /Users/ashesh/.agent-deck/skills/pool/skill-creator/scripts/quick_validate.py /Users/ashesh/claude-deck/skills/agent-deck
python3 /Users/ashesh/.agent-deck/skills/pool/skill-creator/scripts/quick_validate.py /Users/ashesh/claude-deck/skills/session-share
python3 /Users/ashesh/.agent-deck/skills/pool/skill-creator/scripts/quick_validate.py /Users/ashesh/.agent-deck/skills/pool/gsd-conductor
```

2. **Verify $SKILL_DIR pattern presence:**
- `skills/agent-deck/SKILL.md` must contain "SKILL_DIR" (already has it)
- `skills/session-share/SKILL.md` must contain "SKILL_DIR" (added by Plan 01, Task 1)
- `~/.agent-deck/skills/pool/gsd-conductor/SKILL.md` does NOT need $SKILL_DIR because it has no executable scripts (only references/). Verify this is still true (no scripts/ directory).

3. **Verify no hardcoded absolute user paths in any SKILL.md:**
```bash
grep -rn "/Users/" /Users/ashesh/claude-deck/skills/agent-deck/SKILL.md || echo "OK: no absolute paths in agent-deck"
grep -rn "/Users/" /Users/ashesh/claude-deck/skills/session-share/SKILL.md || echo "OK: no absolute paths in session-share"
grep -rn "/Users/" /Users/ashesh/.agent-deck/skills/pool/gsd-conductor/SKILL.md || echo "OK: no absolute paths in gsd-conductor"
```

4. **Verify frontmatter compliance for all three:**
- agent-deck: name, description (required), metadata.compatibility (optional, present)
- session-share: name, description (required), metadata.compatibility (optional, present)
- gsd-conductor: name, description (required), no compatibility needed

If any validation fails, fix the issue in the relevant file and re-run validation.

Note: This task depends on Plan 01 having completed (it reads session-share after Plan 01's changes). Since both plans are Wave 1, the executor should run Plan 01 first or verify session-share has been updated. If session-share hasn't been updated yet, skip its $SKILL_DIR check and note it in the summary.
  </action>
  <verify>
    <automated>python3 /Users/ashesh/.agent-deck/skills/pool/skill-creator/scripts/quick_validate.py /Users/ashesh/claude-deck/skills/agent-deck && python3 /Users/ashesh/.agent-deck/skills/pool/skill-creator/scripts/quick_validate.py /Users/ashesh/claude-deck/skills/session-share && python3 /Users/ashesh/.agent-deck/skills/pool/skill-creator/scripts/quick_validate.py /Users/ashesh/.agent-deck/skills/pool/gsd-conductor && echo "ALL THREE SKILLS PASS VALIDATION"</automated>
  </verify>
  <done>
- All three skills pass quick_validate.py validation
- agent-deck and session-share SKILL.md files contain $SKILL_DIR path resolution pattern
- gsd-conductor SKILL.md correctly has no $SKILL_DIR (no scripts/ directory)
- No hardcoded absolute user paths in any SKILL.md
  </done>
</task>

</tasks>

<verification>
Run validation suite on all three skills:
```bash
python3 /Users/ashesh/.agent-deck/skills/pool/skill-creator/scripts/quick_validate.py /Users/ashesh/claude-deck/skills/agent-deck
python3 /Users/ashesh/.agent-deck/skills/pool/skill-creator/scripts/quick_validate.py /Users/ashesh/claude-deck/skills/session-share
python3 /Users/ashesh/.agent-deck/skills/pool/skill-creator/scripts/quick_validate.py /Users/ashesh/.agent-deck/skills/pool/gsd-conductor
```

Verify path resolution patterns:
```bash
grep -c "SKILL_DIR" /Users/ashesh/claude-deck/skills/agent-deck/SKILL.md
grep -c "SKILL_DIR" /Users/ashesh/claude-deck/skills/session-share/SKILL.md
ls /Users/ashesh/.agent-deck/skills/pool/gsd-conductor/scripts/ 2>/dev/null || echo "No scripts dir (correct)"
```

Verify no hardcoded paths:
```bash
grep -rn "/Users/" /Users/ashesh/claude-deck/skills/*/SKILL.md /Users/ashesh/.agent-deck/skills/pool/gsd-conductor/SKILL.md 2>/dev/null || echo "No hardcoded paths found"
```
</verification>

<success_criteria>
- GSD conductor SKILL.md content reflects GSD v1.22.4 (current lifecycle commands, correct agent types)
- All three skills pass quick_validate.py
- Path resolution uses $SKILL_DIR where scripts exist, no hardcoded absolute paths
- references/gsd-internals.md agent table is current
</success_criteria>

<output>
After completion, create `.planning/phases/01-skills-reorganization/01-02-SUMMARY.md`
</output>
