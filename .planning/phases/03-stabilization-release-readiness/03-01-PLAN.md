---
phase: 03-stabilization-release-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "any files with dead code (TBD by scan)"
autonomous: true
requirements: [STAB-02, STAB-03, STAB-04, STAB-05]

must_haves:
  truths:
    - "golangci-lint run exits with code 0 and zero warnings"
    - "go test -race -count=1 ./... passes all packages with zero failures"
    - "go build succeeds for darwin/amd64, darwin/arm64, linux/amd64, linux/arm64"
    - "No obviously unused exports, orphaned files, or stale artifacts remain from this milestone's changes"
  artifacts:
    - path: "cmd/agent-deck/main.go"
      provides: "Clean build entry point"
    - path: "internal/session/"
      provides: "Clean session package (no dead code)"
  key_links:
    - from: "golangci-lint run"
      to: "exit code 0"
      via: "default linters including unused"
      pattern: "exit 0"
    - from: "go test -race"
      to: "all packages pass"
      via: "race detector enabled"
      pattern: "ok.*PASS"
---

<objective>
Verify all quality gates pass and remove any dead code or stale artifacts from the codebase.

Purpose: Confirm the codebase is clean after Phases 1-2 (skills reorganization + testing) and remove anything that should not ship.
Output: A codebase that passes golangci-lint, full test suite, and cross-platform builds with no dead code.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-stabilization-release-readiness/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run quality gates and verify clean state</name>
  <files>No files modified (verification only)</files>
  <action>
Run all three quality gates and confirm they pass. These are verification steps, not fix steps. Per research, all three are already clean.

1. Run `golangci-lint run` in the project root. Confirm exit code 0 and zero warning lines in output.
2. Run `go test -race -count=1 ./...` and confirm all packages pass.
3. Run cross-platform build verification loop:
   ```bash
   for os in darwin linux; do
     for arch in amd64 arm64; do
       GOOS=$os GOARCH=$arch go build -o /dev/null ./cmd/agent-deck && echo "$os/$arch: OK" || echo "$os/$arch: FAILED"
     done
   done
   ```
   Confirm all 4 lines show "OK".

If any gate fails unexpectedly, fix the issue before proceeding. Per research, this is unlikely. The default golangci-lint config is intentional: do NOT add a .golangci.yml config file or enable extra linters. Do NOT attempt to refactor high-complexity functions (home.go:Update() complexity is pre-existing and out of scope).
  </action>
  <verify>
    <automated>golangci-lint run && go test -race -count=1 ./... && for os in darwin linux; do for arch in amd64 arm64; do GOOS=$os GOARCH=$arch go build -o /dev/null ./cmd/agent-deck || exit 1; done; done && echo "ALL GATES PASS"</automated>
  </verify>
  <done>golangci-lint exits 0 with zero warnings, all test packages pass with race detector, all 4 platform builds succeed</done>
</task>

<task type="auto">
  <name>Task 2: Dead code scan and stale artifact removal</name>
  <files>Any files with dead code identified by scan</files>
  <action>
Scan for dead code and stale artifacts introduced or left behind by this milestone's work (Phases 1-2). Per research, scope this to the milestone's changes, not a codebase-wide audit.

1. Run `go vet ./...` to confirm static analysis is clean.
2. Run `gofmt -l .` and confirm no files need formatting.
3. Scan for stale artifacts:
   - Check for any `.migrated` JSON files that should have been cleaned up
   - Check for orphaned test helpers not used by any test file
   - Check for unused imports in files modified during Phases 1-2 (skill files, test files)
   - Look for any TODO/FIXME comments added during this milestone that should be resolved
4. For any stale artifacts or dead code found:
   - Remove the artifact
   - Run `go build ./...` immediately after each removal to confirm nothing breaks
   - Run `go test -race -count=1 ./...` after all removals

Per research pitfalls:
- Do NOT remove functions that appear unused but may be called via reflection or interface satisfaction
- Only remove code that `unused` linter flags or that `grep -r` confirms has zero references across the entire codebase
- After every removal, run `go build ./...` immediately. Never batch removals without intermediate builds.
- If no dead code or stale artifacts are found, that is a valid outcome. Document "scan complete, no issues found" and move on.
  </action>
  <verify>
    <automated>go vet ./... && test -z "$(gofmt -l .)" && golangci-lint run && go test -race -count=1 ./... && echo "DEAD CODE SCAN COMPLETE"</automated>
  </verify>
  <done>Dead code scan completed. Either: (a) stale artifacts removed and all gates still pass, or (b) no stale artifacts found and documented as clean. No unused imports, orphaned files, or dead code from this milestone remain.</done>
</task>

</tasks>

<verification>
All quality gates pass:
- `golangci-lint run` exits 0 with zero warnings
- `go test -race -count=1 ./...` passes all packages
- All 4 cross-platform builds succeed
- `go vet ./...` and `gofmt -l .` both clean
- No dead code or stale artifacts remain from this milestone
</verification>

<success_criteria>
STAB-02: golangci-lint zero warnings confirmed
STAB-03: Full test suite passes with race detector
STAB-04: All 4 target platform builds succeed
STAB-05: Dead code scan complete, any findings removed or documented as clean
</success_criteria>

<output>
After completion, create `.planning/phases/03-stabilization-release-readiness/03-01-SUMMARY.md`
</output>
