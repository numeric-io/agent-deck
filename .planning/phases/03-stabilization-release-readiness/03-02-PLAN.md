---
phase: 03-stabilization-release-readiness
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - CHANGELOG.md
autonomous: true
requirements: [STAB-06]

must_haves:
  truths:
    - "CHANGELOG.md has an [Unreleased] entry documenting all changes from this milestone"
    - "Changelog entry uses Keep a Changelog 1.1.0 format with Added and Changed sections"
    - "Changelog content accurately reflects user-visible changes only (no planning docs)"
    - "make ci passes as the final release gate"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Milestone changelog entry"
      contains: "[Unreleased]"
  key_links:
    - from: "CHANGELOG.md"
      to: "Phase 1-2 deliverables"
      via: "Accurate description of changes"
      pattern: "skill-creator format|session lifecycle tests|skills runtime tests"
---

<objective>
Write the CHANGELOG.md entry for this milestone and run the final release gate.

Purpose: Document all user-visible changes from the skills reorganization and testing milestone so developers and users understand what changed.
Output: Updated CHANGELOG.md with [Unreleased] entry, final make ci gate passed.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-stabilization-release-readiness/03-RESEARCH.md
@.planning/phases/03-stabilization-release-readiness/03-01-SUMMARY.md
@CHANGELOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write CHANGELOG.md entry for milestone</name>
  <files>CHANGELOG.md</files>
  <action>
Add an `[Unreleased]` section at the top of CHANGELOG.md (after the header, before the `[0.21.1]` entry). Use Keep a Changelog 1.1.0 format.

The entry should document user/developer-visible changes only. Do NOT mention planning documents, STATE.md, or internal GSD workflow files.

Use this template from the research, adjusted for any findings from Plan 01 (check 03-01-SUMMARY.md for any removals):

```markdown
## [Unreleased]

### Added
- Add session lifecycle tests covering start, stop, fork, and attach operations with tmux verification.
- Add status lifecycle tests for sleep/wake detection and SQLite persistence round-trips.
- Add skills runtime tests verifying on-demand skill loading, pool skill discovery, and project skill application.

### Changed
- Reformat agent-deck and session-share SKILL.md files to official Anthropic skill-creator format with proper frontmatter.
- Add $SKILL_DIR path resolution to session-share skill for plugin cache compatibility.
- Register session-share skill in marketplace.json for independent discoverability.
- Update GSD conductor skill content in pool directory with current lifecycle documentation.
```

If Plan 01 removed dead code or stale artifacts, add a `### Removed` section documenting what was removed.

Per research pitfalls:
- Do NOT document .planning/ files or internal process artifacts
- Do NOT bump the version (out of scope per REQUIREMENTS.md)
- Only document changes that affect the user or developer experience
  </action>
  <verify>
    <automated>head -30 CHANGELOG.md | grep -q "\[Unreleased\]" && head -30 CHANGELOG.md | grep -q "### Added" && head -30 CHANGELOG.md | grep -q "### Changed" && echo "CHANGELOG FORMAT OK"</automated>
  </verify>
  <done>CHANGELOG.md has [Unreleased] section with Added and Changed categories documenting all milestone changes in Keep a Changelog format. No planning docs or internal artifacts mentioned.</done>
</task>

<task type="auto">
  <name>Task 2: Final release gate with make ci</name>
  <files>No files modified (verification only)</files>
  <action>
Run the full CI pipeline as the final release readiness gate:

1. Run `make ci` which executes lefthook pre-push (lint + test + build in parallel).
2. Confirm all three parallel checks pass:
   - golangci-lint run
   - go test -race -count=1 ./...
   - go build -o /dev/null ./cmd/agent-deck/
3. Run the 4-platform cross-platform build verification one more time:
   ```bash
   for os in darwin linux; do
     for arch in amd64 arm64; do
       GOOS=$os GOARCH=$arch go build -o /dev/null ./cmd/agent-deck && echo "$os/$arch: OK" || echo "$os/$arch: FAILED"
     done
   done
   ```

This is the final gate confirming the codebase is ready to tag a release (when the user decides to bump the version).
  </action>
  <verify>
    <automated>make ci && for os in darwin linux; do for arch in amd64 arm64; do GOOS=$os GOARCH=$arch go build -o /dev/null ./cmd/agent-deck || exit 1; done; done && echo "RELEASE GATE PASSED"</automated>
  </verify>
  <done>make ci passes all checks. All 4 cross-platform builds succeed. Codebase is release-ready.</done>
</task>

</tasks>

<verification>
- CHANGELOG.md contains [Unreleased] section with accurate milestone documentation
- make ci passes (lint + test + build)
- All 4 cross-platform builds pass
- No version bump (deferred per requirements)
</verification>

<success_criteria>
STAB-06: CHANGELOG.md updated with [Unreleased] entry documenting all milestone changes in Keep a Changelog format.
Final gate: make ci green + 4-platform builds pass.
</success_criteria>

<output>
After completion, create `.planning/phases/03-stabilization-release-readiness/03-02-SUMMARY.md`
</output>
